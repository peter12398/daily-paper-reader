---
title: Semantic Class Distribution Learning for Debiasing Semi-Supervised Medical Image Segmentation
title_zh: 语义类别分布学习用于去偏半监督医学图像分割
authors: "Yingxue Su, Yiheng Zhong, Keying Zhu, Zimu Zhang, Zhuoru Zhang, Yifang Wang, Yuxin Zhang, Jingxin Liu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05202v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 用于半监督医学图像分割的深度学习
tldr: 针对医学图像分割中像素级标注昂贵且类别严重失衡的问题，本文提出语义类别分布学习（SCDL）框架。该框架通过学习结构化的类条件特征分布，利用类分布双向对齐（CDBA）和语义锚点约束（SAC）来缓解监督和表示偏差。在Synapse和AMOS数据集上的实验表明，SCDL显著提升了分割性能，尤其是在少数类上达到了SOTA水平。
selection_source: fresh_fetch
motivation: 医学图像分割面临标注成本高和类别分布极度不均的挑战，导致少数类特征在表示中被主导类别淹没。
method: 提出SCDL框架，通过类分布双向对齐（CDBA）和语义锚点约束（SAC）来学习并对齐结构化的类条件特征分布。
result: 在Synapse和AMOS数据集上，该方法显著提升了整体及少数类的分割精度，达到了当前最先进的性能水平。
conclusion: SCDL作为一个即插即用的模块，有效缓解了半监督学习中的表示偏差，为不平衡医学图像分割提供了可靠方案。
---

## 摘要
医学图像分割对于计算机辅助诊断至关重要。然而，密集的像素级标注既耗时又昂贵，且医学数据集通常表现出严重的类别不平衡。这种不平衡导致少数类结构在特征表示中被主导类淹没，阻碍了判别性特征的学习，使得可靠的分割变得尤为具有挑战性。为此，我们提出了语义类别分布学习（SCDL）框架，这是一个即插即用的模块，通过学习结构化的类别条件特征分布来减轻监督和表示偏差。SCDL 集成了类别分布双向对齐（CDBA）以将嵌入与可学习的类别代理对齐，并利用语义锚点约束（SAC）使用有标注数据来引导代理。在 Synapse 和 AMOS 数据集上的实验表明，SCDL 在整体和类别级指标上均显著提高了分割性能，尤其是在少数类上取得了强劲的提升，达到了最先进的结果。我们的代码已发布在 https://github.com/Zyh55555/SCDL。

## Abstract
Medical image segmentation is critical for computer-aided diagnosis. However, dense pixel-level annotation is time-consuming and expensive, and medical datasets often exhibit severe class imbalance. Such imbalance causes minority structures to be overwhelmed by dominant classes in feature representations, hindering the learning of discriminative features and making reliable segmentation particularly challenging. To address this, we propose the Semantic Class Distribution Learning (SCDL) framework, a plug-and-play module that mitigates supervision and representation biases by learning structured class-conditional feature distributions. SCDL integrates Class Distribution Bidirectional Alignment (CDBA) to align embeddings with learnable class proxies and leverages Semantic Anchor Constraints (SAC) to guide proxies using labeled data. Experiments on the Synapse and AMOS datasets demonstrate that SCDL significantly improves segmentation performance across both overall and class-level metrics, with particularly strong gains on minority classes, achieving state-of-the-art results. Our code is released at https://github.com/Zyh55555/SCDL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Class Distribution Learning for Debiasing Semi-Supervised Medical Image Segmentation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：医学图像分割中存在严重的**类别不平衡（长尾分布）**和**标注稀缺**问题。
*   **研究背景**：在半监督医学图像分割（SSMIS）中，大器官（头部类）占据绝大部分像素，而小器官（尾部类）样本极少。这导致了两个偏差：
    1.  **监督偏差**：模型生成的伪标签和梯度更新倾向于大器官，进一步抑制了小器官的学习。
    2.  **表示偏差**：特征空间中，头部类的特征非常紧凑，而尾部类的特征会向头部类漂移，导致边界模糊。
*   **整体含义**：本文提出了一种名为 **SCDL（语义类别分布学习）** 的框架，旨在通过在特征空间学习结构化的类条件分布，纠正半监督学习中的表示偏差，提升对少数类（小器官）的分割精度。

### 2. 核心方法论
SCDL 是一个**即插即用**的模块，主要包含两个核心组件：

*   **类分布双向对齐 (CDBA)**：
    *   **建模**：将每个语义类别表示为特征空间中的一个可学习的高斯分布（代理分布），包含均值和方差。
    *   **双向对齐**：
        *   **嵌入到代理 (E2P)**：通过软分配（Soft Assignment），让每个像素特征向其对应的类别代理靠拢。
        *   **代理到嵌入 (P2E)**：优化代理分布，使其能够更好地判别属于该类的特征，增强类间区分度。
    *   **特征增强**：通过对代理分布进行采样，生成“分布加权先验”和“中心相似性先验”，并将其注入解码器，为分割提供结构化的语义引导。
*   **语义锚点约束 (SAC)**：
    *   **核心思想**：由于代理分布是随机初始化的，可能偏离真实语义。SAC 利用有标注数据提取每个类别的特征平均值作为“语义锚点”。
    *   **对齐**：强制可学习的代理分布均值与对应的语义锚点对齐，确保代理分布捕捉到真实的类别语义，防止其在训练中漂移。

### 3. 实验设计
*   **数据集**：
    1.  **Synapse**：包含30个CT扫描，13种器官类别。实验设置 20% 的数据有标注。
    2.  **AMOS**：大规模腹部多器官基准，包含15种类别。实验设置极端的 5% 数据有标注。
*   **Benchmark 与对比方法**：
    *   对比了多种 SOTA（最先进）方法，包括通用半监督框架（GenericSSL, SimiS）、类别不平衡专用方法（CLD, DHC）以及最新的梯度感知方法（GA-MagicNet, GA-CPS）。
    *   **评价指标**：Dice 相似系数 (DSC) 和平均表面距离 (ASD)。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A40 GPU** 上完成。
*   **参数设置**：Batch size 设置为 4，SCDL 模块的权重衰减（weight decay）为 1e-4。
*   **训练时长**：文中未明确提及具体的训练总时长，但指出其配置遵循了基准模型（如 DHC, GA-CPS）的标准设置。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了全面测试。
    *   提供了**逐类别（Per-class）**的 Dice 分数对比，这对于评估长尾分布下的性能至关重要。
    *   进行了**消融实验**，分别验证了 CDBA 和 SAC 模块的有效性。
*   **充分性评价**：实验设计较为充分且客观。通过在不同基准模型（如 DHC 和 GA-CPS）上集成 SCDL，证明了该模块的通用性和鲁棒性。特别是 AMOS 数据集上 5% 标注的极端实验，有力地证明了其在稀缺标注下的去偏能力。

### 6. 主要结论与发现
*   **性能提升**：SCDL 在所有数据集上均显著优于基准模型。在 Synapse 上，SCDL-DHC 比原始 DHC 提升了 3.01% 的平均 Dice。
*   **尾部类突破**：在处理极小器官（如肾上腺、门静脉）时表现尤为突出。例如在 AMOS 数据集上，将某些基准模型无法识别（0% Dice）的肾上腺分割精度提升到了 30% 以上。
*   **边界优化**：通过 SAC 约束，显著降低了 ASD 指标，意味着分割结果的几何边界更加精准。

### 7. 优点
*   **即插即用**：作为一个轻量级模块，可以轻松集成到现有的各种分割网络中。
*   **分布级建模**：不同于传统的损失重采样或重加权，SCDL 从特征分布的底层逻辑入手，解决了表示层面的不平衡问题。
*   **双向对齐机制**：不仅让特征找类别，也让类别找特征，这种双向反馈增强了模型在半监督场景下的稳定性。

### 8. 不足与局限
*   **计算开销**：虽然模块轻量，但引入了高斯分布采样和多重先验计算，可能会在推理或训练初期增加一定的计算负担。
*   **超参数敏感性**：代理分布的初始化和采样噪声比例可能需要针对不同数据集进行微调。
*   **应用范围**：目前仅在 CT 图像的多器官分割上验证，对于其他模态（如 MRI、超声）或病灶分割（形态极不规则）的效果尚待进一步验证。

（完）
