---
title: Structure and Progress Aware Diffusion for Medical Image Segmentation
title_zh: 面向医学图像分割的结构与进度感知扩散模型
authors: "Siyuan Song, Guyue Hu, Chenglong Li, Dengdi Sun, Zhe Jin, Jin Tang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07889v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 深度学习医学图像分割任务
tldr: 针对医学图像分割中边界模糊和结构理解的挑战，本文提出了一种结构与进度感知的扩散模型（SPAD）。该模型包含语义集中扩散（ScD）和边界中心扩散（BcD），并由进度感知调度器（PaS）调节。通过在训练初期关注粗粒度的形态和语义结构，后期逐渐转向细粒度的边界调整，SPAD实现了从粗到精的分割过程，有效提升了对复杂病灶和解剖结构的分割精度。
selection_source: fresh_fetch
motivation: 现有方法在训练中同时学习粗结构和细边界，但模糊的边界噪声在早期阶段会干扰模型对稳定解剖结构的理解。
method: 提出SPAD框架，结合语义集中扩散以保留上下文锚点，以及边界中心扩散来模糊不确定边界，并由进度感知调度器控制学习重心转移。
result: 该方法通过从粗粒度形态到细粒度轮廓的渐进式学习，显著提高了模型在复杂医学图像上的分割性能。
conclusion: SPAD通过引入结构感知和进度调节机制，优化了扩散模型在医学图像分割中的特征提取与边界细化能力。
---

## 摘要
医学图像分割对于计算机辅助诊断至关重要，这需要理解粗粒度的形态和语义结构，并刻画精细的边界。医学图像中的形态和语义结构是目标理解的有益且稳定的线索。然而，由于病变重叠、标注不确定性等原因，医学目标（如肿瘤和病灶）的精细边界通常是模糊且多噪点的，这使得其作为早期监督信号并不可靠。然而，现有方法在整个训练过程中同时学习粗粒度结构和精细边界。在本文中，我们提出了一种用于医学图像分割的结构与进度感知扩散模型（SPAD），它由语义集中扩散（ScD）和边界中心扩散（BcD）组成，并由进度感知调度器（PaS）进行调节。具体而言，语义集中扩散引入了锚点保留的目标扰动，该扰动会干扰医学目标内的像素，但保留未改变的区域作为语义锚点，从而鼓励模型从周围的语义上下文中推断带噪声的目标区域。边界中心扩散引入了进度感知边界噪声，它模糊了不可靠且模糊的边界，从而迫使模型关注粗粒度但稳定的解剖形态和全局语义。此外，进度感知调度器逐渐调节 ScD 和 BcD 的噪声强度，形成一种由粗到精的扩散范式，鼓励在早期目标理解阶段关注粗粒度的形态和语义结构，并在后期的轮廓调整阶段逐渐转向精细的目标边界。

## Abstract
Medical image segmentation is crucial for computer-aided diagnosis, which necessitates understanding both coarse morphological and semantic structures, as well as carving fine boundaries. The morphological and semantic structures in medical images are beneficial and stable clues for target understanding. While the fine boundaries of medical targets (like tumors and lesions) are usually ambiguous and noisy since lesion overlap, annotation uncertainty, and so on, making it not reliable to serve as early supervision. However, existing methods simultaneously learn coarse structures and fine boundaries throughout the training process. In this paper, we propose a structure and progress-aware diffusion (SPAD) for medical image segmentation, which consists of a semantic-concentrated diffusion (ScD) and a boundary-centralized diffusion (BcD) modulated by a progress-aware scheduler (PaS). Specifically, the semantic-concentrated diffusion introduces anchor-preserved target perturbation, which perturbs pixels within a medical target but preserves unaltered areas as semantic anchors, encouraging the model to infer noisy target areas from the surrounding semantic context. The boundary-centralized diffusion introduces progress-aware boundary noise, which blurs unreliable and ambiguous boundaries, thus compelling the model to focus on coarse but stable anatomical morphology and global semantics. Furthermore, the progress-aware scheduler gradually modulates noise intensity of the ScD and BcD forming a coarse-to-fine diffusion paradigm, which encourage focusing on coarse morphological and semantic structures during early target understanding stages and gradually shifting to fine target boundaries during later contour adjusting stages.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structure and Progress Aware Diffusion for Medical Image Segmentation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
医学图像分割（如肿瘤或器官提取）面临双重挑战：一方面需要理解**粗粒度的形态和语义结构**（如器官的整体形状、位置关系），这些信息通常是稳定且可靠的；另一方面需要刻画**精细的边界**，但由于病灶重叠、对比度低及标注不确定性，边界往往充满噪声且不可靠。
**研究动机：** 现有的深度学习方法（包括传统的 U-Net 和新兴的扩散模型）通常在整个训练过程中同时学习粗结构和细边界。作者认为这种“一刀切”的范式在训练早期会受到模糊边界噪声的干扰，导致模型难以捕捉稳定的解剖特征。因此，研究核心在于如何构建一种**由粗到精（Coarse-to-fine）**的渐进式学习路径。

### 2. 方法论：核心思想与关键技术
论文提出了**结构与进度感知扩散模型（SPAD）**，其核心思想是通过受控的噪声扰动和进度调度，引导模型先学习稳定的结构，再精细化边界。

*   **语义集中扩散 (ScD)：**
    *   **技术细节：** 在训练时，随机选择图像中的目标区域进行扰动，但保留一小部分像素作为“语义锚点（Anchors）”。
    *   **作用：** 这种“目标内扰动”强迫模型利用周围的上下文信息和保留的锚点来推断被破坏的区域，从而增强模型对全局语义结构和解剖合理性的推理能力。
*   **边界中心扩散 (BcD)：**
    *   **技术细节：** 利用边缘检测算子（如 Canny）提取目标边界，并在边界区域注入特定强度的噪声。
    *   **作用：** 在训练早期模糊掉不可靠的边界细节，迫使模型将注意力集中在更稳定的内部形态上，避免过早陷入对噪声边界的过拟合。
*   **进度感知调度器 (PaS)：**
    *   **算法流程：** 引入一个随训练轮次（Epoch）变化的衰减函数 $\sigma_p = \sigma_{max} / (1 + \beta \cdot p)$。
    *   **逻辑：** 在训练初期，噪声强度大，模型主要学习粗结构；随着训练推进，噪声逐渐减小，模型开始接收更清晰的边界信号，从而实现从结构理解到轮廓刻画的平滑过渡。

### 3. 实验设计
*   **数据集：**
    1.  **AMD-SD 数据集：** 高质量 OCT 图像，包含 5 类视网膜病变结构（SRF, IRF, PED 等）。
    2.  **CXRS 数据集：** 高分辨率胸部 X 射线图像，包含 31 类解剖结构（肋骨、锁骨、肺部等）。
*   **Benchmark 与对比方法：**
    *   **传统架构：** U-Net, Swin-Unet, TransUnet。
    *   **医疗适配模型：** MSA (Medical SAM Adapter)。
    *   **结构感知模型：** D-LKA Net, DSC-AMP。
    *   **扩散模型基准：** CCDM (Stochastic segmentation baseline)。

### 4. 资源与算力
*   **训练时长：** 论文给出了效率对比表。在相同硬件环境下，SPAD 的训练时间约为 21.6 小时（AMD-SD 数据集），推理时间约为 0.78 小时。
*   **算力设备：** 文中**未明确说明**具体的 GPU 型号和数量，但提到其计算开销与基础扩散模型（CCDM）几乎持平，仅增加了微小的掩码生成和调度计算开销。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了两个不同模态（OCT 和 X-ray）、不同任务（病灶分割与解剖结构分割）的大型数据集。
*   **消融实验：** 详细验证了 ScD、BcD 以及 PaS 各个组件的贡献。
*   **敏感性分析：** 针对调度器衰减率 $\beta$、语义锚点比例 $r$ 进行了多组对比实验。
*   **充分性评价：** 实验设计较为全面，通过可视化分析展示了模型在“结构定位”和“边界细化”上的具体改进，对比实验涵盖了从 CNN 到 Transformer 再到扩散模型的多种主流路线，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升：** SPAD 在 AMD-SD 上达到了 71.51% mIoU，在 CXRS 上达到了 71.55% mIoU，均显著优于现有 SOTA 方法。
*   **渐进学习的有效性：** 实验证明，先抑制边界噪声、优先学习结构，再逐渐放开边界细节的策略，能有效提升模型在处理模糊医学目标时的鲁棒性。
*   **结构推理：** ScD 机制显著改善了模型对不连续或碎片化目标的识别能力。

### 7. 优点（亮点）
*   **认知启发：** 模拟了人类医生“先看大体位置，再定精确边缘”的诊断逻辑。
*   **解耦设计：** 巧妙地将结构学习和边界细化通过不同的扰动策略进行解耦，并通过时间轴（进度）重新整合。
*   **即插即用：** 提出的扰动策略和调度器可以较容易地集成到其他基于扩散模型的分割框架中。

### 8. 不足与局限
*   **类别差异：** 在 AMD-SD 数据集的 SHRM 类别（小而碎片化的目标）上，性能提升不明显甚至略有下降，说明该方法对极小目标的精细处理仍有优化空间。
*   **计算开销：** 尽管比基准扩散模型提升微小，但相比 U-Net 等非生成式模型，扩散模型固有的多次迭代去噪过程导致其推理速度仍然较慢，限制了实时应用场景。
*   **参数依赖：** 边界扰动的宽度目前是固定的，对于极细长的解剖结构，固定的扰动范围可能会覆盖掉过多的有效信息。

（完）
