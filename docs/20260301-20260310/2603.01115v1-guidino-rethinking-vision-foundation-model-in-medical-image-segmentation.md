---
title: "GuiDINO: Rethinking Vision Foundation Model in Medical Image Segmentation"
title_zh: GuiDINO：重新思考医学图像分割中的视觉基础模型
authors: "Zhuonan Liang, Wei Guo, Jie Gan, Yaxuan Song, Runnan Chen, Hang Chang, Weidong Cai"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01115v1"
tags: ["query:med-fm"]
score: 10.0
evidence: 从DINOv3提取视觉特征表示用于医学分割
tldr: 针对视觉基础模型在医学图像分割中因领域偏移导致的性能受限问题，本文提出GuiDINO框架。该框架将DINOv3重新定位为视觉引导生成器，通过轻量化的TokenBook机制将基础模型特征转化为空间引导掩码，用于调节医学专用分割网络的特征激活。GuiDINO在保留专用架构归纳偏置的同时注入了强大的视觉先验，显著提升了分割精度和边界鲁棒性，为基础模型在医学领域的应用提供了新思路。
selection_source: fresh_fetch
motivation: 视觉基础模型在医学图像任务中存在领域偏移，直接微调或简单适配难以充分发挥其特征表示能力。
method: 引入TokenBook机制将DINOv3特征转化为空间引导掩码，并结合引导监督损失和边界铰链损失来增强医学分割主干网络的特征激活。
result: 在多种医学数据集上，GuiDINO显著提升了分割质量和边界准确性，且支持通过LoRA进行参数高效的适配。
conclusion: GuiDINO证明了将基础模型作为视觉引导生成器而非直接主干网络，是提升医学图像分割性能的一种高效且实用的方案。
---

## 摘要
视觉基础模型在医学图像分析中的应用日益广泛。由于领域偏移，这些预训练模型在未经完全微调或轻量化适配的情况下，往往无法很好地对齐医学图像分割的需求。我们提出了 GuiDINO，这是一个将原生基础模型重新定位为下游分割任务视觉引导生成器的框架。GuiDINO 从 DINOv3 中提取视觉特征表示，并通过一种轻量级的 TokenBook 机制将其转换为空间引导掩码，该机制聚合了 token 与原型之间的相似度。该引导掩码对多个分割主干网络中的特征激活进行门控处理，从而在保留医学专用架构的归纳偏置和效率的同时，注入基础模型的先验知识。训练过程依赖于一种引导监督目标损失，使引导掩码与真实区域对齐，并可选择性地通过关注边界的合页损失来增强细微结构的清晰度。GuiDINO 还支持通过在 DINOv3 引导主干上应用 LoRA 来实现参数高效适配。在多种医学数据集和 nnUNet 风格的推理中，GuiDINO 持续提升了分割质量和边界鲁棒性，为微调提供了一种实用的替代方案，并为基础模型如何更好地服务于医学视觉提供了新视角。代码可在 https://github.com/Hi-FishU/GuiDINO 获取。

## Abstract
Foundation vision models are increasingly adopted in medical image analysis. Due to domain shift, these pretrained models misalign with medical image segmentation needs without being fully fine-tuned or lightly adapted. We introduce GuiDINO, a framework that repositions native foundation model to acting as a visual guidance generator for downstream segmentation. GuiDINO extracts visual feature representation from DINOv3 and converts them into a spatial guide mask via a lightweight TokenBook mechanism, which aggregates token-prototype similarities. This guide mask gates feature activations in multiple segmentation backbones, thereby injecting foundation-model priors while preserving the inductive biases and efficiency of medical dedicated architectures. Training relies on a guide supervision objective loss that aligns the guide mask to ground-truth regions, optionally augmented by a boundary-focused hinge loss to sharpen fine structures. GuiDINO also supports parameter-efficient adaptation through LoRA on the DINOv3 guide backbone. Across diverse medical datasets and nnUNet-style inference, GuiDINO consistently improves segmentation quality and boundary robustness, suggesting a practical alternative to fine-tuning and offering a new perspective on how foundation models can best serve medical vision. Code is available at https://github.com/Hi-FishU/GuiDINO

---

## 论文详细总结（自动生成）

以下是对论文《GuiDINO: Rethinking Vision Foundation Model in Medical Image Segmentation》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉基础模型（VFMs，如 DINOv3）在自然图像领域表现卓越，但在医学图像分割中面临**领域偏移（Domain Shift）**和**语义不匹配**的问题。
*   **研究动机**：
    *   直接全量微调 VFM 消耗算力巨大且医学标注数据稀缺。
    *   医学专用架构（如 nnUNet）具有良好的归纳偏置，但缺乏大规模预训练带来的空间先验。
    *   作者试图探讨：如何不将 VFM 作为直接的特征提取主干，而是将其作为“引导者”来辅助医学专用网络，从而兼顾基础模型的泛化能力和医学网络的专业性。

### 2. 论文提出的方法论：GuiDINO 框架
*   **核心思想**：将 DINOv3 定位为**视觉引导生成器（Visual Guidance Generator）**。它不直接参与语义分类，而是生成一个空间引导掩码（Guide Mask），用于“门控”医学分割主干网络的特征激活。
*   **关键技术细节**：
    *   **TokenBook 机制**：从冻结的 DINOv3 中提取密集 Token 特征 $T_i$，通过计算 Token 与一组可学习原型 $P$ 的相似度，并结合权重 $\alpha_i$ 聚合生成空间引导图。公式表达为：$G(x) = \sum \alpha_i \cdot sim(T_i, P)$。
    *   **特征门控（Gating）**：生成的引导掩码作用于分割主干网络（如 UNet 或 nnWNet）的特征层，引导模型关注感兴趣区域。
    *   **参数高效适配**：支持在 DINOv3 上应用 LoRA（低秩自适应），以极小的参数代价微调基础模型以适应医学模态。
*   **损失函数**：采用复合损失 $L = L_{seg} + \lambda L_{guide}$。其中 $L_{guide}$ 强制引导掩码与真实标签（GT）对齐，并可选配边界合页损失（Hinge Loss）以锐化边缘。

### 3. 实验设计
*   **数据集**：
    *   **Kvasir-SEG**：结肠镜息肉分割（1,000 张图像）。
    *   **ISIC 2017**：皮肤病变分割（2,750 张图像）。
    *   **TN3K**：甲状腺超声结节分割（3,493 张图像）。
*   **Benchmark 与对比方法**：
    *   对比了经典及前沿模型：nnUNet、SwinUNet、H2Former、U-KAN、nnWNet 以及同样基于 DINO 的 SegDINO。
    *   评估指标：Dice 系数（DSC）、交并比（IoU）和 95% 豪斯多夫距离（HD95）。

### 4. 资源与算力
*   **硬件环境**：使用了单张 **NVIDIA R6000 Ada GPU (40GB 显存)**。
*   **训练细节**：
    *   输入分辨率：256x256。
    *   训练时长：400 个 Epoch。
    *   优化器：AdamW，初始学习率 1e-4（LoRA 部分为 5e-4）。
    *   推理风格：遵循 nnUNet 的推理流程。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个不同模态（内窥镜、皮肤镜、超声）的数据集上进行了验证。
    *   **消融实验**：分别在 UNet 和 nnWNet 两种不同性质的主干上测试了 GuiDINO 的增益。
    *   **变体对比**：对比了有无 LoRA 适配、有无 GuiDINO 引导的多种组合。
*   **客观性**：所有实验结果均基于 3 次不同随机种子的运行平均值，确保了结果的稳健性和可重复性。实验设计较为全面，涵盖了性能对比、架构兼容性和可视化分析。

### 6. 论文的主要结论与发现
*   **性能提升**：GuiDINO 在所有测试数据集上均显著优于基准模型。例如，在 Kvasir-SEG 上，GuiDINO-W 的 IoU 达到了 84.82%，优于 nnWNet 原型。
*   **鲁棒性增强**：在主干网络表现不佳的场景下（如 nnWNet 在 TN3K 上的表现），GuiDINO 展现了极强的“补救”能力，大幅提升了指标。
*   **边界优化**：通过 HD95 指标的下降证明了该方法在处理复杂边界和低对比度目标时的优越性。
*   **引导作用**：可视化结果证实，DINO 生成的引导掩码能准确捕捉目标的粗略位置，帮助主干网络聚焦。

### 7. 优点（亮点）
*   **视角独特**：打破了“基础模型必须做 Backbone”的思维定式，将其作为辅助引导信息，有效缓解了领域偏移问题。
*   **轻量且灵活**：TokenBook 机制非常轻量，且该框架可以无缝集成到现有的各种医学分割网络中。
*   **即插即用**：不需要对基础模型进行大规模重训，通过冻结权重或 LoRA 即可实现高性能。

### 8. 不足与局限
*   **LoRA 增益不稳**：实验显示 LoRA 在某些数据集（如 TN3K）上的提升并不一致，说明基础模型的适配策略仍需针对特定任务精细调整。
*   **维度限制**：目前实验主要集中在 2D 医学图像上，对于 3D 医疗影像（如 CT/MRI 序列）的适用性尚未在文中深入探讨。
*   **超参数敏感性**：引导损失的权重 $\lambda$ 以及原型的数量等超参数可能需要针对不同任务进行手动调优。

（完）
