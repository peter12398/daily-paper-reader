---
title: Spatial Autoregressive Modeling of DINOv3 Embeddings for Unsupervised Anomaly Detection
title_zh: 基于 DINOv3 嵌入的空间自回归建模用于无监督异常检测
authors: "Ertunc Erdil, Nico Schulthess, Guney Tombak, Ender Konukoglu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02974v1"
tags: ["query:med-fm"]
score: 10.0
evidence: 直接利用 DINOv3 嵌入进行异常检测
tldr: 本研究针对无监督异常检测（UAD）中DINO模型忽略图像块空间关系及内存开销大的问题，提出一种基于DINOv3嵌入的2D自回归（AR）建模框架。该方法利用卷积神经网络显式捕捉块间空间依赖，取代了传统的存储库或原型表示，实现了紧凑的参数化正态分布建模。在BMAD医疗影像数据集上的实验表明，该方法在保持竞争力的检测性能的同时，显著降低了推理时间和内存占用，为高效、轻量化的异常检测提供了新思路。
selection_source: fresh_fetch
motivation: 现有方法忽略了DINO嵌入间的空间上下文关系，且依赖高内存开销的存储库或原型表示，导致推理效率低下。
method: 提出一种使用2D自回归卷积神经网络显式建模DINOv3块嵌入空间依赖性的简单高效框架。
result: 在BMAD医疗影像基准测试中，该方法在大幅减少内存和计算开销的同时，取得了与现有先进方法相当的检测性能。
conclusion: 通过自回归模型显式捕捉空间上下文，可以有效提升无监督异常检测的效率并降低对存储资源的依赖。
---

## 摘要
DINO 模型提供了丰富的补丁级（patch-level）表示，近期在无监督异常检测（UAD）中表现出色。大多数现有方法从“正常”图像中提取补丁嵌入并对其进行独立建模，忽略了补丁之间的空间和邻域关系。这隐含地假设自注意力机制和位置编码已在每个补丁嵌入中充分编码了上下文信息。此外，常态分布通常被建模为记忆库（memory banks）或基于原型的表示，这需要存储大量特征并在推理时进行昂贵的比较，从而导致巨大的内存和计算开销。在这项工作中，我们通过提出一个简单且高效的框架来解决这两个局限性，该框架使用二维自回归（2D AR）模型显式地建模补丁嵌入之间的空间和上下文依赖关系。我们的方法不是存储嵌入或聚类原型，而是通过自回归卷积神经网络（AR CNN）学习常态分布的紧凑参数化模型。在测试时，异常检测简化为通过网络的单次前向传递，从而实现快速且内存高效的推理。我们在包含三个医学影像数据集的 BMAD 基准上评估了我们的方法，并将其与包括近期基于 DINO 的方法在内的现有工作进行了比较。实验结果表明，显式建模空间依赖关系在实现具有竞争力的异常检测性能的同时，显著降低了推理时间和内存需求。代码可在项目页面获取：https://eerdil.github.io/spatial-ar-dinov3-uad/。

## Abstract
DINO models provide rich patch-level representations that have recently enabled strong performance in unsupervised anomaly detection (UAD). Most existing methods extract patch embeddings from ``normal'' images and model them independently, ignoring spatial and neighborhood relationships between patches. This implicitly assumes that self-attention and positional encodings sufficiently encode contextual information within each patch embedding. In addition, the normative distribution is often modeled as memory banks or prototype-based representations, which require storing large numbers of features and performing costly comparisons at inference time, leading to substantial memory and computational overhead. In this work, we address both limitations by proposing a simple and efficient framework that explicitly models spatial and contextual dependencies between patch embeddings using a 2D autoregressive (AR) model. Instead of storing embeddings or clustering prototypes, our approach learns a compact parametric model of the normative distribution via an AR convolutional neural network (CNN). At test time, anomaly detection reduces to a single forward pass through the network and enables fast and memory-efficient inference. We evaluate our method on the BMAD benchmark, which comprises three medical imaging datasets, and compare it against existing work including recent DINO-based methods. Experimental results demonstrate that explicitly modeling spatial dependencies achieves competitive anomaly detection performance while substantially reducing inference time and memory requirements. Code is available at the project page: https://eerdil.github.io/spatial-ar-dinov3-uad/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Spatial Autoregressive Modeling of DINOv3 Embeddings for Unsupervised Anomaly Detection》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：无监督异常检测（UAD）在医疗影像中至关重要，旨在仅通过健康样本学习“正态分布”。
*   **核心问题**：
    1.  **空间关系缺失**：现有的基于 DINO 嵌入的方法（如 AnomalyDINO）通常将图像块（patch）嵌入视为独立样本，忽略了它们在 2D 网格中的空间和邻域依赖关系。
    2.  **计算开销巨大**：主流方法依赖“记忆库”（Memory Banks）或复杂的聚类原型，在推理时需要进行昂贵的最近邻搜索，导致内存占用高、推理速度慢。
*   **研究动机**：利用 DINOv3 强大的表示能力，结合空间自回归建模，在提升检测精度的同时实现极高的推理效率。

### 2. 核心方法论
*   **核心思想**：将图像块嵌入的联合分布建模为一个 2D 自回归（AR）过程，通过捕捉补丁间的条件依赖关系来学习健康解剖结构的统计规律。
*   **关键技术细节**：
    *   **特征提取**：使用预训练的 DINOv3-S 模型提取 $H_p \times W_p \times D$ 的补丁嵌入网格。
    *   **自回归分解**：将联合概率 $p(F)$ 分解为逐位置的条件概率乘积：$p(F) = \prod_{i,j} p(F_{i,j} | F_{<i,j})$，其中 $F_{<i,j}$ 遵循光栅扫描顺序（从左到右，从上到下）。
    *   **掩码卷积（Masked CNN）**：为了实现并行训练和推理，采用类似 PixelCNN 的掩码卷积核，确保每个位置的预测仅依赖于“过去”的嵌入。
    *   **空洞卷积（Dilated Convolutions）**：引入空洞卷积以扩大感受野。这能防止模型仅通过极局部的插值来预测（这会导致对异常不敏感），从而捕捉更长距离的空间上下文。
    *   **异常评分**：在测试时，异常分数即为负对数似然（NLL）。由于采用 CNN 架构，只需一次前向传递即可获得全图异常图。

### 3. 实验设计
*   **数据集（BMAD 基准）**：
    1.  **BraTS2021**：脑部 MRI 图像。
    2.  **BTCV + LiTs**：腹部 CT 图像（肝脏）。
    3.  **RESC**：视网膜 OCT 图像。
*   **对比方法**：涵盖了重建类（DRAEM, UTRAD）、学生-教师类（RD4AD, STFPM）、特征分布类（PaDiM, CFLOW, CFA）以及基于记忆库/DINO 的最新方法（PatchCore, AnomalyDINO, DPMM）。
*   **评价指标**：像素级 AUROC 和 AUPR，以及推理时间和内存消耗。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A6000 GPU (48 GB VRAM)** 上运行。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 64，学习率为 $10^{-3}$。
*   **算力说明**：文中未明确给出总训练时长，但强调了推理效率。在 RESC 数据集上，该方法的推理时间仅为 **20ms**，内存占用仅 **0.2GB**，远低于 AnomalyDINO（585ms, 11.3GB）。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同模态的医疗数据集上进行了测试，每个实验运行 3 个随机种子并取平均值。
*   **消融实验**：
    *   对比了标准卷积与空洞卷积的效果。
    *   对比了自回归模型与双向（Bidirectional）上下文模型。
    *   对比了在嵌入空间建模与直接在图像空间建模的差异。
    *   测试了不同规模的 DINOv3 主干网络（Small vs. 7B）。
*   **充分性评价**：实验设计较为全面，涵盖了性能、效率、架构变体等多个维度，对比基准包含了该领域最先进的算法，结果具有客观性。

### 6. 主要结论与发现
*   **性能领先**：在 BraTS2021 数据集上，该方法（空洞卷积版）取得了最高的 AUPR（72.42%），在其他数据集上也保持了极具竞争力的 AUROC。
*   **效率飞跃**：相比于其他基于 DINO 的方法，该方法在推理速度上快了 **30-50 倍**，内存需求降低了 **50-100 倍**。
*   **空间建模的重要性**：显式建模空间依赖（尤其是通过空洞卷积扩大感受野）能有效提升结构化解剖数据（如大脑）的异常检测能力。
*   **模型规模收益递减**：将 DINOv3-S 升级为 7B 大模型对性能提升微乎其微，说明小规模主干网络已能提供足够的特征表示。

### 7. 优点
*   **极高效率**：摆脱了记忆库的束缚，实现了单次前向传递的快速推理，适合临床实时应用。
*   **紧凑性**：通过参数化 CNN 学习分布，模型文件极小，无需存储海量特征。
*   **空间感知**：相比独立建模补丁的方法，更好地利用了医疗影像中固有的解剖结构信息。

### 8. 不足与局限
*   **数据集差异性**：空洞卷积在脑部 MRI 上效果显著，但在视网膜 OCT 等局部特征更关键的数据集上，性能提升不明显甚至略有下降。
*   **对主干网络的依赖**：性能高度依赖于 DINOv3 的预训练质量，若基础模型在某些特定医学模态上表现不佳，该框架也会受限。
*   **异常类型覆盖**：虽然在 BMAD 上表现良好，但对于极微小或缺乏空间结构特征的异常，自回归建模的优势可能无法充分发挥。

（完）
