---
title: "LightMedSeg: Lightweight 3D Medical Image Segmentation with Learned Spatial Anchors"
title_zh: LightMedSeg：基于学习空间锚点的轻量级三维医学图像分割
authors: "Kavyansh Tyagi, Vishwas Rathi, Puneet Goyal"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07228v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 轻量级 3D 医学图像分割架构
tldr: 针对临床AI中3D医学图像分割对内存和延迟的严苛要求，本文提出LightMedSeg。该模型采用轻量化UNet架构，通过学习空间锚点和FiLM调制引入解剖先验，并利用Ghost卷积和纹理感知路由减少计算冗余。在仅有0.48M参数和14.64 GFLOPs的情况下，实现了接近重型Transformer模型的分割精度，为资源受限场景提供了高效且稳健的解决方案。
selection_source: fresh_fetch
motivation: 现有的Transformer模型在3D医学图像分割中参数量大、计算成本高且泛化能力有限，难以满足临床部署需求。
method: 提出一种集成解剖先验的轻量化UNet架构，利用锚点调制、局部结构先验和深度卷积等技术优化特征提取与融合。
result: 模型仅需0.48M参数和14.64 GFLOPs，其Dice评分与参数量巨大的Transformer基准模型非常接近。
conclusion: LightMedSeg是一种可部署且数据高效的3D医学图像分割方案，成功平衡了计算效率与分割精度。
---

## 摘要
精确且高效的三维医学图像分割对于临床人工智能至关重要，模型必须在严格的内存、延迟和数据可用性约束下保持可靠。基于 Transformer 的方法虽然取得了较高的准确率，但存在参数量过大、计算量（FLOPs）高以及泛化能力有限的问题。我们提出了 LightMedSeg，这是一种模块化的 UNet 风格分割架构，它将解剖学先验与自适应上下文建模相结合。锚点条件的 FiLM 调制实现了感知解剖结构的特征校准，而局部结构先验模块和纹理感知路由则动态地将表征能力分配给边界丰富的区域。通过 Ghost 卷积和深度可分离卷积最小化了计算冗余，并通过带有锚点相对空间位置偏差的学习跳跃路由自适应地融合多尺度特征。尽管仅需 0.48M 参数和 14.64 GFLOPs，LightMedSeg 的分割准确率与重量级 Transformer 基准模型相比仅相差几个 Dice 点。因此，LightMedSeg 是一种可部署且数据高效的三维医学图像分割解决方案。代码将在论文接收后公开。

## Abstract
Accurate and efficient 3D medical image segmentation is essential for clinical AI, where models must remain reliable under stringent memory, latency, and data availability constraints. Transformer-based methods achieve strong accuracy but suffer from excessive parameters, high FLOPs, and limited generalization. We propose LightMedSeg, a modular UNet-style segmentation architecture that integrates anatomical priors with adaptive context modeling. Anchor-conditioned FiLM modulation enables anatomy-aware feature calibration, while a local structural prior module and texture-aware routing dynamically allocate representational capacity to boundary-rich regions. Computational redundancy is minimized through ghost and depthwise convolutions, and multi-scale features are adaptively fused via a learned skip router with anchor-relative spatial position bias. Despite requiring only 0.48M parameters and 14.64~GFLOPs, LightMedSeg achieves segmentation accuracy within a few Dice points of heavy transformer baselines. Therefore, LightMedSeg is a deployable and data-efficient solution for 3D medical image segmentation. Code will be released publicly upon acceptance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LightMedSeg** 的轻量级三维医学图像分割架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证高分割精度的同时，显著降低三维医学图像分割模型的参数量、计算量（FLOPs）和推理延迟，以满足临床 AI 在有限内存和实时性要求下的部署需求。
*   **研究背景**：
    *   **CNN 局限性**：传统的 U-Net 及其变体虽然局部特征提取能力强，但感受野有限，难以捕捉全局解剖依赖关系。
    *   **Transformer 局限性**：虽然 nnFormer、UNETR 等模型通过全局自注意力机制提升了精度，但其参数量巨大（动辄上百兆）、计算成本高昂且推理缓慢。
    *   **现有方法不足**：大多采用静态跳跃连接和统一的计算分配，忽略了医学图像中解剖先验的重要性以及不同区域（如边界与内部）对计算资源需求的差异。

### 2. 方法论：核心思想与关键技术
LightMedSeg 采用模块化的 UNet 风格设计，核心思想是**“解剖先验引导的自适应计算”**。
*   **GhostConv3D Patch Embedding**：使用 Ghost 卷积替代标准卷积作为输入层，通过廉价的操作生成更多特征图，大幅减少初始阶段的参数。
*   **全局锚点检测器 (Global Anchor Detector)**：预测 $K=8$ 个代表关键解剖位置的归一化空间坐标（锚点），以此作为全局上下文的紧凑表示。
*   **局部结构先验模块 (LSPM)**：
    *   **纹理路由图**：识别高频强度变化的边界区域。
    *   **自适应特征混合**：根据区域复杂程度，动态分配计算资源给“细节路径”或“平滑路径”。
*   **锚点条件 FiLM 调制**：在编码器中，利用预测的锚点坐标通过 FiLM（特征线性调制）对特征图进行缩放和偏移，引入全局解剖位置信息。
*   **学习型多尺度跳跃融合**：放弃固定跳跃连接，改用学习型路由器，通过 Softmax 权重自适应地融合来自不同尺度的编码器特征。
*   **自适应解码器与空间位置偏差 (SPB)**：解码器引入基于锚点相对距离的空间位置偏差，增强模型对解剖结构的感知能力。

### 3. 实验设计
*   **数据集**：
    *   **BraTS (Brain Tumor Segmentation)**：脑肿瘤分割，包含 411 个训练案例和 73 个测试案例。
    *   **ACDC (Automatic Cardiac Diagnosis Challenge)**：心脏多结构分割。
*   **Benchmark (基准方法)**：
    *   重型 Transformer 模型：nnFormer, UNETR, SwinUNETR, UNETR++。
    *   轻量级/混合模型：SegFormer3D, TransBTS, CoTr, LeViT-UNet。
*   **评价指标**：Dice 评分（准确率）、参数量 (Params)、计算量 (FLOPs)、推理延迟和内存占用。

### 4. 资源与算力
*   **硬件环境**：推理测试在 **NVIDIA RTX 5080 GPU** 和 **Ryzen 9 9950x CPU** 上进行。
*   **训练细节**：使用 AdamW 优化器，初始学习率 $2 \times 10^{-4}$，训练 300 个 epoch，batch size 为 8。
*   **推理速度**：在 RTX 5080 上处理 $128^3$ 体积数据仅需 **13.7 ms**，在 CPU 上需 505.4 ms。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流医学影像数据集上进行了完整的对比实验。
*   **消融实验**：非常充分。作者分别验证了 LSPM 模块、空间锚点、学习型跳跃路由器、Ghost 卷积以及锚点数量 $K$ 对最终性能的影响。
*   **客观性**：对比了从 2016 年到 2024 年的多种主流模型，涵盖了纯 CNN、纯 Transformer 及混合架构，实验对比维度全面（精度 vs. 效率）。

### 6. 主要结论与发现
*   **极致轻量化**：LightMedSeg 仅有 **0.48M** 参数和 **14.64 GFLOPs**。
*   **性能强劲**：在 BraTS 上达到 83.4% 的 Dice 分数，在 ACDC 上达到 91.24%。虽然比参数量大 300 倍的 nnFormer 低约 1-3 个百分点，但远优于许多同类轻量级模型。
*   **关键模块贡献**：消融实验显示 **LSPM（局部结构先验）** 对精度的提升贡献最大（约 2.93%），证明了针对边界区域进行特殊处理的有效性。

### 7. 优点与亮点
*   **效率极高**：参数量仅为 SegFormer3D 的 1/9，UNETR++ 的 1/88，非常适合嵌入式医疗设备。
*   **解剖感知**：通过学习空间锚点而非简单的自注意力，以极低的成本引入了全局空间上下文。
*   **自适应性**：纹理感知路由和学习型跳跃连接使模型能根据输入图像的内容动态调整特征融合策略。

### 8. 不足与局限
*   **精度差距**：尽管效率极高，但在追求极致精度的场景下，与最先进的重型 Transformer 模型相比仍有微小差距（约 1-2% Dice）。
*   **数据集覆盖**：目前仅在脑部和心脏数据集上验证，尚未在腹部多器官或更小病灶（如肺结节）数据集上广泛测试。
*   **超参数敏感性**：锚点数量 $K$ 的选择对性能有一定影响，虽然文中给出了建议值，但在不同任务中可能需要微调。

（完）
