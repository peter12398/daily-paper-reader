---
title: "TubeMLLM: A Foundation Model for Topology Knowledge Exploration in Vessel-like Anatomy"
title_zh: TubeMLLM：一种用于管状解剖结构拓扑知识探索的基础模型
authors: "Yaoyu Liu, Minghui Zhang, Xin You, Hanxiao Zhang, Yun Gu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09217v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 血管类解剖结构的基础模型
tldr: TubeMLLM是一个针对医学管状解剖结构设计的统一多模态大模型。针对现有模型在处理复杂拓扑结构时易出现断裂或误连的问题，该模型通过自然语言提示集成拓扑先验，并结合共享注意力架构实现视觉与拓扑知识的对齐。研究还构建了TubeMData基准，实验证明该模型在跨模态零样本迁移、拓扑一致性维护及鲁棒性方面均达到领先水平。
selection_source: fresh_fetch
motivation: 针对医学管状结构建模中存在的拓扑不一致性（如人工断连和虚假合并）以及对数据集偏移敏感的问题。
method: 提出TubeMLLM模型，通过自然语言提示集成拓扑先验，利用共享注意力架构对齐视觉与拓扑表征，并引入自适应损失加权策略。
result: 在15个数据集上表现优异，显著降低了拓扑误差，并在X射线血管造影等未见模态上展现出卓越的零样本迁移能力。
conclusion: TubeMLLM作为一种基础模型，有效提升了医学管状结构的拓扑感知理解与生成能力，具有极强的泛化性和鲁棒性。
---

## 摘要
由于复杂的拓扑结构和对数据集偏移的敏感性，对医学管状解剖结构进行建模具有挑战性。因此，特定任务模型通常会遇到拓扑不一致的问题，包括人为断连和伪合并。受多模态大语言模型（MLLMs）在零样本泛化方面的潜力启发，我们提出了 TubeMLLM，这是一个将结构化理解与可控生成相结合的统一基础模型，专门用于医学管状解剖结构。通过显式的自然语言提示集成拓扑先验，并在共享注意力架构中将其与视觉表示对齐，TubeMLLM 显著增强了拓扑感知能力。此外，我们构建了 TubeMData，这是一个包含全面拓扑中心任务的先驱性多模态基准，并引入了一种自适应损失加权策略，以在训练期间强调拓扑关键区域。在 15 个不同数据集上的广泛实验证明了我们的优越性。在定量分析方面，TubeMLLM 实现了最先进的分布外性能，大幅减少了彩色眼底摄影中的全局拓扑差异（与基准模型相比，将 $β_{0}$ 数量误差从 37.42 降低到 8.58）。值得注意的是，TubeMLLM 在未见过的 X 射线血管造影上表现出卓越的零样本跨模态迁移能力，在实现 67.50% Dice 分数的同时，将 $β_{0}$ 误差显著降低至 1.21。TubeMLLM 还能对模糊、噪声和低分辨率等退化保持鲁棒性。此外，在拓扑感知理解任务中，该模型在评估掩码拓扑质量方面达到了 97.38% 的准确率，显著优于标准的视觉语言基准模型。

## Abstract
Modeling medical vessel-like anatomy is challenging due to its intricate topology and sensitivity to dataset shifts. Consequently, task-specific models often suffer from topological inconsistencies, including artificial disconnections and spurious merges. Motivated by the promise of multimodal large language models (MLLMs) for zero-shot generalization, we propose TubeMLLM, a unified foundation model that couples structured understanding with controllable generation for medical vessel-like anatomy. By integrating topological priors through explicit natural language prompting and aligning them with visual representations in a shared-attention architecture, TubeMLLM significantly enhances topology-aware perception. Furthermore, we construct TubeMData, a pionner multimodal benchmark comprising comprehensive topology-centric tasks, and introduce an adaptive loss weighting strategy to emphasize topology-critical regions during training. Extensive experiments on fifteen diverse datasets demonstrate our superiority. Quantitatively, TubeMLLM achieves state-of-the-art out-of-distribution performance, substantially reducing global topological discrepancies on color fundus photography (decreasing the $β_{0}$ number error from 37.42 to 8.58 compared to baselines). Notably, TubeMLLM exhibits exceptional zero-shot cross-modality transferring ability on unseen X-ray angiography, achieving a Dice score of 67.50% while significantly reducing the $β_{0}$ error to 1.21. TubeMLLM also maintains robustness against degradations such as blur, noise, and low resolution. Furthermore, in topology-aware understanding tasks, the model achieves 97.38% accuracy in evaluating mask topological quality, significantly outperforming standard vision-language baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《TubeMLLM: A Foundation Model for Topology Knowledge Exploration in Vessel-like Anatomy》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：医学中的血管类结构（如眼底视网膜血管、X射线冠状动脉造影）具有极细长、分支多、存在环路等复杂拓扑特性。传统的图像到图像（I2I）分割模型（如 nnUNet）在面对数据集偏移或模态变化时，极易产生拓扑错误（如人为断开或虚假合并）。
*   **核心问题**：如何构建一个既能理解复杂拓扑先验，又能在不同模态间具有强泛化能力的统一基础模型，以实现高保真度的血管结构生成与分析。

### 2. 方法论
TubeMLLM 采用了一种将**结构化理解**与**可控生成**耦合的统一架构：
*   **核心思想**：利用多模态大模型（MLLM）的推理能力，通过显式的自然语言提示（Prompt）注入拓扑知识（如连通性、环路的定义），并在共享注意力架构中对齐视觉与文本表征。
*   **关键技术细节**：
    *   **混合 Transformer 架构 (Mixture-of-Transformers)**：包含生成分支（基于 Rectified Flow 的图像生成）和理解分支（基于 ViT 的视觉问答）。两分支在每一层通过共享注意力机制交换信息。
    *   **拓扑先验注入**：不同于以往仅使用短语标签，TubeMLLM 使用长文本描述拓扑规则（如“8-连通域”、“不许断裂”），引导模型学习。
    *   **自适应损失加权 (Adaptive Loss Weighting)**：在训练生成分支时，模型根据预测掩码与真值的误差图（Error Map），为拓扑关键区域或易错区域的视觉 Token 分配更高的权重（公式：$w_i = 1 + \lambda e_i$），从而强化对细小血管的关注。
    *   **Rectified Flow 生成**：在 VAE 潜空间内建模速度向量，通过流匹配（Flow-matching）目标函数实现高质量掩码生成。

### 3. 实验设计
*   **数据集 (TubeMData)**：构建了一个包含 15 个公开数据集的大型基准。
    *   **彩色眼底摄影 (CFP)**：包括 FIVES, RETA, RVD, STARE（训练）；AFIO, ARIA, CHASEDB1, DRIVE, IOSTAR, LES-AV（测试）。
    *   **X射线血管造影 (XRA)**：包括 DCA1, Dr-SAM, FS-CAD, XCAV（训练）；XCAD（测试）。
*   **任务设计**：
    1.  **拓扑保真生成**：包括从原图生成掩码及对已有缺陷掩码进行“拓扑精炼”（Refinement）。
    2.  **拓扑感知理解**：以 VQA 形式进行，如判断是否存在环路、计算连通域数量、对比两个掩码的拓扑质量。
*   **对比方法**：
    *   **特定任务模型**：nnUNet 及其拓扑增强变体（clDice, SR, cbDice, CAL）。
    *   **血管专用架构**：FR-UNet, AFN。
    *   **基础模型**：SAM3, MedicalSAM3, Bagel。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量以及总训练时长。仅提到模型是在预训练模型（Bagel）的基础上进行微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 个数据集，总样本量约 5.2 万个。
*   **充分性评价**：实验设计非常充分且具有挑战性。
    *   **分布外 (OOD) 测试**：测试集与训练集严格分离，验证了模型的泛化性。
    *   **零样本 (Zero-shot) 迁移**：在完全未见过的 XRA 模态上进行测试，这是衡量基础模型能力的关键指标。
    *   **鲁棒性实验**：针对模糊、噪声、低分辨率输入进行了专项评估。
    *   **消融实验**：验证了拓扑中心任务（T-T）对提升分割性能的贡献。
*   **客观性**：引入了 $\beta_0$ 数量误差和 $\beta_0$ 匹配误差等专门的拓扑评估指标，比单纯的 Dice 分数更客观地反映了血管连通性质量。

### 6. 主要结论与发现
*   **拓扑误差大幅降低**：在 CFP 数据集上，将 $\beta_0$ 数量误差从基准的 37.42 降至 8.58。
*   **卓越的跨模态能力**：在零样本 XRA 任务中，Dice 达到 67.50%，且 $\beta_0$ 误差仅为 1.21，远超传统模型（传统模型在跨模态下几乎失效）。
*   **理解能力极强**：在评估掩码拓扑质量的任务中，准确率高达 97.38%，证明模型确实“理解”了什么是正确的拓扑。
*   **鲁棒性**：在图像质量退化的情况下，TubeMLLM 的性能下降远小于 nnUNet。

### 7. 优点
*   **范式创新**：将血管分割从单纯的像素分类提升到了“语言引导的拓扑推理+生成”的高度。
*   **统一性**：一个模型同时解决了理解（问答）和生成（分割）任务，且两者互为促进。
*   **显式先验**：通过自然语言提示解决了传统损失函数难以定义的复杂拓扑约束问题。
*   **自适应机制**：误差驱动的 Token 加权有效解决了细长结构在全局损失中被稀释的问题。

### 8. 不足与局限
*   **维度限制**：目前的研究主要集中在 2D 图像，尚未扩展到 3D 医疗影像（如 CT/MRI 血管造影）。
*   **推理开销**：作为基于大语言模型的架构，其推理速度（尤其是生成分支的多步迭代）可能远慢于轻量级的实时分割网络，论文未讨论实时性。
*   **提示词依赖**：模型性能可能受到 Prompt 描述方式的影响，对于非专业用户编写提示词的鲁棒性有待验证。

（完）
