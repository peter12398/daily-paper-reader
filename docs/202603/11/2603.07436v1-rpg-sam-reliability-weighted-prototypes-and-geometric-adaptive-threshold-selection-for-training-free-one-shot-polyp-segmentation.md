---
title: "RPG-SAM: Reliability-Weighted Prototypes and Geometric Adaptive Threshold Selection for Training-Free One-Shot Polyp Segmentation"
title_zh: RPG-SAM：用于无需训练的单样本息肉分割的可信度加权原型与几何自适应阈值选择
authors: "Weikun Lin, Yunhao Bai, Yan Wang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07436v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 将基础模型（SAM）应用于单样本医学图像分割
tldr: "针对息肉分割标注成本高的问题，本文提出RPG-SAM框架，实现无需训练的单样本分割。该方法引入可靠性加权原型挖掘和几何自适应阈值选择，分别解决支持图像区域异质性与查询响应异质性，并结合迭代细化优化边界。实验显示其在Kvasir数据集上mIoU提升了5.56%，为医学图像分割提供了高效方案。"
selection_source: fresh_fetch
motivation: 现有的单样本分割方法忽略了支持图像的区域异质性和查询响应的强度异质性，导致分割精度受限。
method: 提出了包含可靠性加权原型挖掘（RWPM）、几何自适应阈值选择（GAS）及迭代细化循环的RPG-SAM框架。
result: "在Kvasir数据集上，该方法相比现有技术实现了5.56%的mIoU性能提升。"
conclusion: RPG-SAM通过系统性地处理多层信息异质性，显著增强了基础模型在无需训练情况下的息肉分割能力。
---

## 摘要
无需训练的单样本分割为专家标注提供了一种可扩展的替代方案，其知识通常从支持图像和基础模型中迁移。然而，现有方法通常以同质化的方式处理支持图像中的所有像素以及查询响应强度。它们忽略了支持图像中的区域异质性和查询中的响应异质性。为了解决这一问题，我们提出了 RPG-SAM，这是一个系统性应对这些异质性差距的框架。具体而言，为了应对区域异质性，我们引入了可信度加权原型挖掘（RWPM），在优先考虑高保真支持特征的同时，利用背景锚点作为对比参考进行噪声抑制。为了应对响应异质性，我们开发了几何自适应选择（GAS），通过评估候选区域的形态共识来动态重新校准二值化阈值。最后，设计了一种迭代细化循环方法来优化解剖边界。通过考虑多层信息异质性，RPG-SAM 在 Kvasir 数据集上实现了 5.56% 的 mIoU 提升。代码将开源。

## Abstract
Training-free one-shot segmentation offers a scalable alternative to expert annotations where knowledge is often transferred from support images and foundation models. But existing methods often treat all pixels in support images and query response intensities models in a homogeneous way. They ignore the regional heterogeity in support images and response heterogeity in query.To resolve this, we propose RPG-SAM, a framework that systematically tackles these heterogeneity gaps. Specifically, to address regional heterogeneity, we introduce Reliability-Weighted Prototype Mining (RWPM) to prioritize high-fidelity support features while utilizing background anchors as contrastive references for noise suppression. To address response heterogeneity, we develop Geometric Adaptive Selection (GAS) to dynamically recalibrate binarization thresholds by evaluating the morphological consensus of candidates. Finally, an iterative refinement loop method is designed to polishes anatomical boundaries. By accounting for multi-layered information heterogeneity, RPG-SAM achieves a 5.56\% mIoU improvement on the Kvasir dataset. Code will be released.

---

## 论文详细总结（自动生成）

以下是对论文《RPG-SAM: Reliability-Weighted Prototypes and Geometric Adaptive Threshold Selection for Training-Free One-Shot Polyp Segmentation》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：息肉分割是早期结直肠癌筛查的关键，但依赖大规模像素级标注的监督学习模型在临床扩展上受限。
*   **核心问题**：现有的“无需训练的单样本分割”方法（如基于 SAM 的方法）存在**同质化偏见（Uniformity Bias）**：
    1.  **区域异质性**：错误地认为支持图像中所有前景像素同等重要，忽略了反光、粘液等噪声区域的负面影响。
    2.  **上下文异质性**：忽略了支持图像背景作为对比参考的价值。
    3.  **强度异质性**：在将相似度热图转化为 Prompt 时使用固定阈值，无法适应不同查询图像中波动的响应强度。
*   **研究目标**：提出 RPG-SAM 框架，通过系统性处理上述异质性，提升单样本息肉分割的精度和鲁棒性。

### 2. 论文提出的方法论
RPG-SAM 包含三个核心模块，旨在实现从支持图像到查询图像的高保真知识迁移：

*   **可信度加权原型挖掘 (RWPM)**：
    *   使用 SLIC 超像素算法提取支持图像的前景和背景原型。
    *   **权重计算**：引入“对比因子”（评估原型与背景的区分度）和“反向纯度因子”（评估原型在查询图中的匹配稳定性）来计算每个原型的可靠性权重。
    *   **背景抑制**：在生成初始热图时，显式利用背景原型作为负锚点来抑制误报激活。
*   **几何自适应阈值选择 (GAS)**：
    *   放弃固定阈值，在一定范围内扫描多个阈值生成候选掩码池。
    *   **几何评分**：根据“加权形态稳固性”（倾向于规则、凸起的解剖形状）和“尺度共识”（排除过小噪声）对候选掩码评分，自动选出最优先验掩码。
*   **先验引导的迭代细化 (PIR)**：
    *   利用 SAM2 的掩码生成能力，以 GAS 选出的掩码为参考。
    *   通过计算当前预测与先验掩码的覆盖率（Coverage）和交并比（IoU），自动在假阴性区域添加正提示点或在假阳性区域添加负提示点，循环优化解剖边界。

### 3. 实验设计
*   **数据集**：使用了四个主流的息肉分割数据集：**Kvasir**、**CVC-ClinicDB**、**CVC-ColonDB** 以及包含三个中心数据的 **PolypGen**。
*   **Benchmark**：以单样本（One-Shot）分割为基准，即每个数据集仅随机选取一张标注图像作为支持样本。
*   **对比方法**：对比了 5 种最先进的（SOTA）方法：SEGIC、PerSAM、OPSAM、Matcher 和 ProtoSAM。
*   **评价指标**：平均交并比 (mIoU) 和平均 Dice 系数 (mDice)。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练时长**：该方法为 **Training-free（无需训练）**，所有推理过程均直接在预训练的基础模型（DINOv2 和 SAM2）上进行，无需任何参数更新或微调。
*   **模型配置**：特征编码器使用 DINOv2 (ViT-L/14)，掩码生成器使用 SAM2。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同来源的数据集（涵盖单中心和多中心场景）上进行了广泛测试。
*   **消融实验**：详细验证了背景抑制、RWPM、GAS 和 PIR 四个组件的增量贡献，证明了每个模块的必要性。
*   **参数敏感性分析**：对 SLIC 的超像素数量、GAS 的扫描范围以及 PIR 的停止阈值等关键超参数进行了多组对比实验。
*   **客观性**：所有对比方法均使用相同的支持-查询图像对和相同的骨干网络，确保了比较的公平性。

### 6. 论文的主要结论与发现
*   **性能提升**：RPG-SAM 在所有数据集上均显著优于现有方法。在 Kvasir 数据集上，mIoU 达到 78.65%，比之前的 SOTA 方法 ProtoSAM 提升了 **5.56%**。
*   **鲁棒性**：在多中心数据集 PolypGen 上表现优异，证明了该方法在应对临床领域偏移（Domain Shift）时的稳定性。
*   **异质性处理的有效性**：实验证明，通过动态调整阈值和加权原型，可以有效解决内窥镜图像中常见的反光干扰和对比度低的问题。

### 7. 优点
*   **无需训练**：极大地降低了临床部署的门槛，无需昂贵的标注和重新训练过程。
*   **系统性解决偏见**：针对单样本分割中长期存在的“同质化假设”提出了针对性的几何和统计学解决方案。
*   **即插即用**：作为一种 Prompt 生成框架，可以较容易地适配到其他视觉基础模型上。
*   **形态学先验**：巧妙地将医学影像的解剖学特征（如息肉的类圆形、凸起性）转化为数学评分函数。

### 8. 不足与局限
*   **时间开销**：由于引入了阈值扫描（GAS）和迭代细化（PIR），其推理速度可能慢于简单的固定阈值方法（虽然文中未详细对比推理耗时）。
*   **视频一致性**：目前主要针对静态图像，尚未充分利用内窥镜视频中的时序连续性。
*   **极端情况**：对于形状极度不规则或与背景纹理完全融合的息肉，基于几何稳固性的评分机制可能失效。

（完）
