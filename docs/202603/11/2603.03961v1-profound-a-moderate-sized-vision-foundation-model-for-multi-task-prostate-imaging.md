---
title: "ProFound: A moderate-sized vision foundation model for multi-task prostate imaging"
title_zh: ProFound：一种用于多任务前列腺成像的中型视觉基础模型
authors: "Yipei Wang, Yinsong Xu, Weixi Yi, Shaheer Ullah Saeed, Natasha Thorley, Alexander Ng, Yukun Zhou, Wen Yan, Dean Barratt, Shonit Punwani, Veeru Kasivisvanathan, Mark Emberton, Daniel C. Alexander, Yipeng Hu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03961v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 针对前列腺多参数MRI的领域专用视觉基础模型
tldr: 针对前列腺癌多参数MRI（mpMRI）自动化诊断中专家标注稀缺且模型通用性差的问题，本文提出了ProFound。这是一个专门针对前列腺影像的视觉基础模型，通过在5000名患者的2.2万个3D MRI卷上进行自监督预训练。在涵盖癌症检测、分级、病灶定位及器官分割等11项下游临床任务的评估中，ProFound表现优于或等同于现有的SOTA模型和通用医疗基础模型，显著提升了前列腺影像分析的效率和准确性。
selection_source: fresh_fetch
motivation: 旨在解决前列腺mpMRI自动化任务中对大规模特定任务标注数据的依赖，以及现有模型临床通用性受限的问题。
method: "利用来自5000名患者的22,000个3D MRI卷，采用多种自监督学习变体预训练了一个领域专用的视觉基础模型。"
result: 在11项下游临床任务的系统评估中，ProFound在性能上持续超越或媲美现有的专用模型及通用医疗视觉基础模型。
conclusion: ProFound证明了领域专用基础模型在处理复杂多任务前列腺影像临床需求方面的巨大潜力和优越性。
---

## 摘要
前列腺癌的许多诊断和治疗临床任务日益依赖多参数核磁共振成像（mpMRI）。自动化这些任务具有挑战性，因为它们需要专家解读，而专家解读难以大规模扩展以利用现代深度学习。尽管现代自动化系统在孤立任务中达到了专家级性能，但由于需要大规模的任务特定标注数据集，其通用临床效用仍然受限。在本文中，我们提出了 ProFound，这是一个针对体积前列腺 mpMRI 的领域专用视觉基础模型。ProFound 在来自多个机构的 5,000 名患者的多样化数据集上，利用多种自监督方法变体进行了预训练，总计包含超过 22,000 个独特的 3D MRI 卷（超过 1,800,000 个 2D 图像切片）。我们在超过 3,000 名独立患者的 11 个下游临床任务中对 ProFound 进行了系统评估，包括前列腺癌检测、Gleason 评分、病灶定位、腺体体积估计、分区及周围结构分割。实验结果表明，微调后的 ProFound 始终优于或与在相同数据上训练/微调的最先进专用模型及现有医学视觉基础模型保持相当的竞争力。

## Abstract
Many diagnostic and therapeutic clinical tasks for prostate cancer increasingly rely on multi-parametric MRI. Automating these tasks is challenging because they necessitate expert interpretations, which are difficult to scale to capitalise on modern deep learning. Although modern automated systems achieve expert-level performance in isolated tasks, their general clinical utility remains limited by the requirement of large task-specific labelled datasets. In this paper, we present ProFound, a domain-specialised vision foundation model for volumetric prostate mpMRI. ProFound is pre-trained using several variants of self-supervised approaches on a diverse, multi-institutional collection of 5,000 patients, with a total of over 22,000 unique 3D MRI volumes (over 1,800,000 2D image slices). We conducted a systematic evaluation of ProFound across a broad spectrum of $11$ downstream clinical tasks on over 3,000 independent patients, including prostate cancer detection, Gleason grading, lesion localisation, gland volume estimation, zonal and surrounding structure segmentation. Experimental results demonstrate that finetuned ProFound consistently outperforms or remains competitive with state-of-the-art specialised models and existing medical vision foundation models trained/finetuned on the same data.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProFound: A moderate-sized vision foundation model for multi-task prostate imaging》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：前列腺癌的诊疗高度依赖多参数核磁共振成像（mpMRI），但深度学习模型在这一领域的应用面临两大瓶颈：一是高质量专家标注数据难以大规模获取；二是现有的通用医疗基础模型（Foundation Models）由于缺乏前列腺特定序列（如T2w, DWI, ADC）的充分表征，在临床任务中表现不佳。
*   **核心问题**：能否通过在中等规模的前列腺专用数据集上进行自监督预训练，构建一个既能兼顾多种临床任务（分割、分类、定位、回归），又对计算资源要求适中的领域专用基础模型？
*   **整体含义**：ProFound 证明了“领域专用”基础模型在医疗影像中的优越性，通过 3D 空间表征学习，显著降低了下游任务对标注数据的依赖。

### 2. 方法论
*   **核心思想**：采用**掩码自编码器（Masked Autoencoder, MAE）**框架，在无标注的前列腺 mpMRI 数据上学习体积表征。
*   **关键技术细节**：
    *   **多模态输入**：将 T2w、高 b 值 DWI 和 ADC 三种序列堆叠为 3 通道的 3D 输入体积。
    *   **两种架构变体**：
        1.  **ProFound-ViT**：基于 Vision Transformer (ViT-B)，将 3D 体积切分为非重叠立方块（Patches），使用 3D 正余弦位置编码。
        2.  **ProFound-Conv**：基于 ConvNeXtV2，保持密集 3D 特征网格，利用稀疏卷积（Sparse Convolutions）仅处理可见体素，提高计算效率。
    *   **预训练目标**：通过重建被掩码（掩码率 0.6-0.75）的图像区域来学习解剖和病理特征。
    *   **下游适配**：支持线性探测（冻结编码器）和全参数微调（使用分层学习率衰减 LLRD）。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：整合了 PICAI、TCIA、ReIMAGINE Risk 等多个机构数据，包含 5,000 名患者、22,000 个 3D 卷（约 180 万张切片）。
    *   **下游评估**：在超过 3,000 名独立患者的数据上进行，涵盖 PROMIS、UCLH 等 5 个独立数据集。
*   **Benchmark 与对比方法**：
    *   **通用基础模型**：RadFM（1600 万对数据预训练）、RadDiag、DINOv2。
    *   **专用模型（从头训练）**：UNet、ResNet18、ViT、ConvNeXt。
*   **评估任务（共 11 项）**：包括 PI-RADS 评分分类、Gleason 分级、病灶分割（LesionSeg）、解剖结构分割（AnatomySeg）、前列腺体积回归、Barzell 区域癌症定位等。

### 4. 资源与算力
*   **训练环境**：
    *   **ProFound-ViT**：使用 4 张 NVIDIA RTX8000 GPU，预训练耗时 **10 天**。
    *   **ProFound-Conv**：使用 4 张 NVIDIA A6000 GPU，预训练耗时 **14 天**。
*   **部署友好性**：模型设计为“中等规模”，旨在确保标准医院硬件即可运行，无需昂贵的计算集群。

### 5. 实验数量与充分性
*   **实验规模**：论文在 11 个不同的临床下游任务上进行了系统评估，这在同类研究中属于非常广泛的覆盖。
*   **数据效率实验**：专门设计了针对极小样本（8 到 128 个样本）的微调实验，验证模型在标注稀缺场景下的鲁棒性。
*   **客观性**：所有实验均运行三次取平均值，并进行了配对 t 检验以验证统计学显著性（p 值）。对比基准涵盖了当前最先进的通用和专用模型，实验设计公平且客观。

### 6. 主要结论与发现
*   **性能领先**：ProFound 在分割任务中显著优于 UNet 和通用基础模型（如 ProFound-Conv 在病灶分割中 Dice 达到 0.429）。
*   **临床一致性**：在 PI-RADS 和 Gleason 分级等序数分类任务中，ProFound 展现了更好的序数一致性（QWK 指标更高），而通用模型往往只能做到二分类区分。
*   **高数据效率**：仅需 32 个标注样本，ProFound 的表现即可超越从头训练的专用模型，极大降低了临床标注成本。
*   **定位能力**：在区域级癌症检测中，ProFound 的特异性优于放射科医生的常规读片水平。

### 7. 优点
*   **领域针对性强**：不同于泛泛的医疗基础模型，它深挖了前列腺 mpMRI 的多序列空间特性。
*   **架构多样性**：同时提供了 CNN 和 Transformer 两种主流架构的预训练权重，适应不同下游任务需求。
*   **开源贡献**：完全开源了模型权重、代码和交互式界面，具有极高的学术和临床实用价值。
*   **严谨的验证**：使用了大量外部独立数据集进行验证，证明了模型的泛化能力。

### 8. 不足与局限
*   **回归任务增益有限**：在体积估计（Volume-Reg）任务中，ProFound 相比简单的 ResNet18 提升并不显著，说明基础模型在简单几何回归任务上的优势不明显。
*   **微调依赖**：虽然数据效率高，但仍需一定量的微调，尚未实现完全的零样本（Zero-shot）临床应用。
*   **数据多样性仍有提升空间**：尽管已有 5000 例数据，但相比自然语言处理或通用视觉领域的规模仍较小，未来需纳入更多罕见病例或不同场强（如 1.5T vs 3T）的数据。

（完）
