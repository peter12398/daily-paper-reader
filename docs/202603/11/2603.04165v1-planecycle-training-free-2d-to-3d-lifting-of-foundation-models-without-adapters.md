---
title: "PlaneCycle: Training-Free 2D-to-3D Lifting of Foundation Models Without Adapters"
title_zh: PlaneCycle：无需训练且无需适配器的基础模型 2D 到 3D 提升
authors: "Yinghong Yu, Guangyuan Li, Jiancheng Yang"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04165v1"
tags: ["query:med-fm"]
score: 10.0
evidence: 使用预训练的 DINOv3 模型进行 3D 分割任务
tldr: 本研究提出PlaneCycle，一种无需训练或适配器的2D到3D模型提升算子。它通过在网络深度中循环分配HW、DW和DH三个正交平面的空间聚合，使预训练的2D基础模型（如DINOv3）能够直接处理3D体积数据。在多个3D分类和分割基准测试中，该方法在不增加参数的情况下展现了强大的3D融合能力，性能接近全量训练模型，为2D基础模型向3D领域的无缝迁移提供了新路径。
selection_source: fresh_fetch
motivation: 现有的2D基础模型扩展到3D通常需要复杂的重新训练、适配器或架构重新设计，缺乏一种简单且无需训练的通用提升方法。
method: PlaneCycle通过在网络层间循环切换HW、DW和DH三个正交平面的空间聚合，在不引入额外参数的情况下实现3D特征融合。
result: 在无需训练的情况下，该方法在多个3D基准测试中优于切片式2D基准，且在线性探测下性能接近全量训练的3D模型。
conclusion: PlaneCycle证明了无需结构修改或重新训练，即可从预训练2D基础模型中有效解锁强大的3D感知和融合能力。
---

## 摘要
大规模 2D 基础模型展现出强大的可迁移表征，但将其扩展到 3D 体积数据通常需要重新训练、使用适配器或进行架构重新设计。我们引入了 PlaneCycle，这是一种无需训练、无需适配器的算子，用于与架构无关的基础模型 2D 到 3D 提升。PlaneCycle 通过在整个网络深度中，将空间聚合循环地分布在正交的 HW、DW 和 DH 平面上，复用了原始预训练的 2D 骨干网络，在保留预训练归纳偏置的同时实现了渐进式 3D 融合。该方法不引入额外参数，且适用于任意 2D 网络。使用预训练的 DINOv3 模型，我们在六个 3D 分类和三个 3D 分割基准测试上评估了 PlaneCycle。在没有任何训练的情况下，提升后的模型展现出内在的 3D 融合能力，并且在线性探测下，其表现优于逐切片的 2D 基准模型和强大的 3D 对应模型，接近完全训练模型的性能。通过全量微调，PlaneCycle 的表现与标准 3D 架构相当，突显了其作为一种无缝且实用的 2D 到 3D 提升算子的潜力。这些结果表明，无需结构修改或重新训练，即可从预训练的 2D 基础模型中解锁 3D 能力。代码可在 https://github.com/HINTLab/PlaneCycle 获取。

## Abstract
Large-scale 2D foundation models exhibit strong transferable representations, yet extending them to 3D volumetric data typically requires retraining, adapters, or architectural redesign. We introduce PlaneCycle, a training-free, adapter-free operator for architecture-agnostic 2D-to-3D lifting of foundation models. PlaneCycle reuses the original pretrained 2D backbone by cyclically distributing spatial aggregation across orthogonal HW, DW, and DH planes throughout network depth, enabling progressive 3D fusion while preserving pretrained inductive biases. The method introduces no additional parameters and is applicable to arbitrary 2D networks. Using pretrained DINOv3 models, we evaluate PlaneCycle on six 3D classification and three 3D segmentation benchmarks. Without any training, the lifted models exhibit intrinsic 3D fusion capability and, under linear probing, outperform slice-wise 2D baselines and strong 3D counterparts, approaching the performance of fully trained models. With full fine-tuning, PlaneCycle matches standard 3D architectures, highlighting its potential as a seamless and practical 2D-to-3D lifting operator. These results demonstrate that 3D capability can be unlocked from pretrained 2D foundation models without structural modification or retraining. Code is available at https://github.com/HINTLab/PlaneCycle.

---

## 论文详细总结（自动生成）

### PlaneCycle：无需训练且无需适配器的基础模型 2D 到 3D 提升论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效地将强大的 2D 视觉基础模型（如 DINOv3）扩展到 3D 体积数据（如 CT、MRI），而无需昂贵的重新训练、引入额外的适配器（Adapters）或修改网络架构。
*   **研究背景**：
    *   2D 基础模型在自然图像上表现卓越，但在医疗 3D 影像中，简单的“逐切片”处理忽略了层间依赖，而直接转换为 3D 架构则会破坏预训练权重且计算量巨大。
    *   现有的 3D 适配器方法通常需要额外参数和监督训练，且在未训练前不具备内在的 3D 感知能力。

#### 2. 方法论
*   **核心思想**：提出 **PlaneCycle** 算子，通过在网络的不同层之间，循环地切换空间聚合的正交平面（HW、DW、DH），使 2D 模型能够逐步融合全量 3D 信息。
*   **关键技术细节**：
    *   **平面循环（Cyclic Planes）**：将 3D 体积特征图按深度方向循环分配给三个正交平面：HW（轴状位）、DW（冠状位）、DH（矢状位）。例如，第 1 层处理 HW，第 2 层处理 DW，以此类推。
    *   **架构无关性**：该算子仅涉及特征图的重塑（Reshape）和转置，不改变模型参数，适用于 ViT 和 CNN。
    *   **全局 Token 处理**：针对 ViT，引入了 `PCm`（全局平均）和 `PCg`（分组平均）策略，通过自适应池化解决不同平面间 Token 长度不匹配的问题。
    *   **位置编码兼容**：对于使用旋转位置嵌入（RoPE）的模型，通过重新索引平面坐标来保持空间一致性。

#### 3. 实验设计
*   **数据集**：
    *   **3D 分类**：MedMNIST+ 中的 6 个数据集（Organ, Nodule, Fracture, Adrenal, Vessel, Synapse），涵盖 CT、MRI 和电镜影像。
    *   **3D 分割**：LIDC（肺结节）和 MMWHS（全心脏）。
*   **基准模型（Benchmarks）**：
    *   **2D Baseline**：逐切片处理。
    *   **3D Baseline**：全体积展平处理。
    *   **对比方法**：R-ACS（针对 CNN 的 3D 转换）、ViViT（视频 Transformer）、DINOv3 官方模型。
*   **评估设置**：零样本特征分析（Zero-training）、线性探测（Linear Probing）和全量微调（Full Fine-tuning）。

#### 4. 资源与算力
*   **算力消耗**：
    *   **线性探测**：ViT-S 约需 2.5GB 显存/10分钟；ViT-L 约需 7.0GB 显存/60分钟。
    *   **全量微调**：显存需求显著增加，ViT-S 需 17GB，ViT-B 需 33GB，ViT-L 需 87GB。
    *   **训练时长**：在 LIDC 分割任务上，PlaneCycle（ViT-L）耗时 16.3 小时，而全 3D 展平方法耗时 36.2 小时。
*   **硬件环境**：使用了 H100 GPU 以及芬兰 CSC 的超算集群（Mahti, Puhti, LUMI）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个不同模态和任务的 3D 数据集，使用了 ViT-S/B/L 三种规模的骨干网络。
*   **充分性评价**：
    *   **客观性**：通过配对 t 检验（Paired t-tests）验证了结果的统计显著性。
    *   **多维度验证**：不仅对比了最终精度，还通过 PCA 可视化和新定义的 `FeatDice` 指标评估了模型在“未训练”状态下的内在 3D 融合能力。
    *   **公平性**：在相同的超参数和解码器架构下对比了 2D、3D 和 PlaneCycle 提升方法。

#### 6. 主要结论与发现
*   **内在 3D 能力**：即使不进行任何训练，PlaneCycle 也能产生比 2D 或 3D 展平方法更具空间一致性的特征。
*   **线性探测优势**：在线性探测设置下，PlaneCycle 显著优于 2D 基准和传统的 3D 转换方法，证明其更好地保留了 2D 基础模型的语义。
*   **微调性能**：在全量微调下，PlaneCycle 的性能与原生 3D 架构相当，但计算复杂度更低（与 2D 相当）。
*   **结论**：无需修改架构或重新训练，即可从 2D 基础模型中“解锁”3D 感知能力。

#### 7. 优点
*   **零参数开销**：不引入任何可学习参数，完全复用预训练权重。
*   **训练友好**：既支持无需训练的直接推理，也支持高效的下游微调。
*   **计算高效**：自注意力机制的复杂度与 2D 切片处理一致，远低于全体积 3D 注意力。
*   **通用性强**：适用于各种 2D 骨干网络（CNN/ViT）和多种 3D 任务。

#### 8. 不足与局限
*   **分割精度限制**：由于 ViT 固有的 16x 下采样和简单的解码器设计，在某些精细分割任务上的空间恢复能力可能受限。
*   **超大规模验证不足**：虽然文中提到支持 7B 规模模型，但实验主要集中在 ViT-L 及以下规模。
*   **平面偏置**：循环策略（如 HW-DW-DH-HW）可能对轴状位（HW）有偏置，虽然这符合医疗影像特点，但在各向同性数据中可能不是最优。

（完）
