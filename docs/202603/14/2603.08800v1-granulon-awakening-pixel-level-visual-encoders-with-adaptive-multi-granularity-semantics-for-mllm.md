---
title: "Granulon: Awakening Pixel-Level Visual Encoders with Adaptive Multi-Granularity Semantics for MLLM"
title_zh: Granulon：利用自适应多粒度语义唤醒多模态大语言模型的像素级视觉编码器
authors: "Junyuan Mao, Qiankun Li, Linghao Meng, Zhicheng He, Xinliang Zhou, Kun Wang, Yang Liu, Yueming Jin"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08800v1"
tags: ["query:med-fm"]
score: 9.0
evidence: 基于DINOv3的多模态大模型，具有自适应粒度的像素级感知
tldr: 针对多模态大模型中CLIP编码器缺乏细粒度理解和DINOv3缺乏高层语义抽象的问题，本文提出Granulon。该模型基于DINOv3，引入文本驱动的粒度控制器和自适应Token聚合模块，实现了根据文本需求动态调整视觉抽象级别。Granulon支持从像素到粗粒度的统一推理，显著提升了模型在复杂视觉任务中的准确性并减少了幻觉。
selection_source: fresh_fetch
motivation: 现有的视觉编码器在全局语义对齐与像素级感知之间存在断层，难以兼顾多粒度的视觉推理需求。
method: 提出Granulon架构，通过文本驱动的粒度控制器和自适应Token聚合模块，实现动态的视觉抽象与语义增强。
result: "实验表明Granulon在相同设置下将准确率提升了约30%，并减少了约20%的幻觉现象。"
conclusion: Granulon成功唤醒了像素级编码器的多粒度语义能力，为构建更精准、低幻觉的多模态大模型提供了有效方案。
---

## 摘要
多模态大语言模型（MLLM）的最新进展在很大程度上依赖于基于 CLIP 的视觉编码器，这类编码器强调全局语义对齐，但在细粒度视觉理解方面表现欠佳。相比之下，DINOv3 提供了强大的像素级感知能力，但缺乏粗粒度语义抽象，导致多粒度推理能力有限。为了填补这一空白，我们提出了 Granulon，一种基于 DINOv3 且具有自适应粒度增强的新型 MLLM。Granulon 引入了一个文本条件的粒度控制器，可根据文本输入的语义范围动态调整视觉抽象水平；此外还引入了一个自适应 Token 聚合模块，通过执行粒度引导的池化和关系感知聚类，生成紧凑且语义丰富的视觉 Token。这种设计使得在单次前向传播中实现统一的“像素到细粒度到粗粒度”推理成为可能。广泛且具可解释性的实验表明，Granulon 将准确率提高了约 30%，并将幻觉减少了约 20%，在相同设置下优于所有视觉编码器。

## Abstract
Recent advances in multimodal large language models largely rely on CLIP-based visual encoders, which emphasize global semantic alignment but struggle with fine-grained visual understanding. In contrast, DINOv3 provides strong pixel-level perception yet lacks coarse-grained semantic abstraction, leading to limited multi-granularity reasoning. To address this gap, we propose Granulon, a novel DINOv3-based MLLM with adaptive granularity augmentation. Granulon introduces a text-conditioned granularity Controller that dynamically adjusts the visual abstraction level according to the semantic scope of the textual input, and an Adaptive Token Aggregation module that performs granularity-guided pooling and relation-aware clustering to produce compact, semantically rich visual tokens. This design enables unified "pixel-to-fine-to-coarse" reasoning within a single forward pass. Extensive and interpretable experiments demonstrate that Granulon improves accuracy by ~30% and reduces hallucination by ~20%, outperforming all visual encoders under identical settings.

---

## 论文详细总结（自动生成）

以下是关于论文《Granulon: Awakening Pixel-Level Visual Encoders with Adaptive Multi-Granularity Semantics for MLLM》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前多模态大语言模型（MLLM）主要依赖 CLIP 类视觉编码器。CLIP 擅长全局语义对齐，但在处理需要细粒度理解的任务时表现较差。相反，像 DINOv3 这样的像素级自监督编码器虽然擅长捕捉局部纹理和几何结构，但缺乏高层语义抽象能力。
*   **核心挑战**：如何在单一模型中统一“像素级细节”与“粗粒度语义”，并根据用户查询的需求动态调整视觉特征的抽象级别。
*   **整体含义**：论文提出了 **Granulon**，通过引入自适应粒度增强机制，成功“唤醒”了 DINOv3 这类像素级编码器的语义潜力，使其能够根据文本指令灵活地在不同视觉尺度上进行推理。

### 2. 方法论
Granulon 架构基于 DINOv3，核心包含两个关键模块：
*   **文本驱动粒度控制器（Granularity Controller）**：
    *   **核心思想**：利用 LLM 的第一层输出作为查询特征，预测当前任务所需的视觉抽象级别（粒度参数 $\alpha$ 控制空间下采样，$\beta$ 控制聚类基数）。
    *   **技术细节**：控制器通过一个轻量级的 MLP 映射文本嵌入，输出一个类别分布，从而决定后续视觉流的聚合模式。例如，宏观问题（“图中有什么动物？”）触发粗粒度模式，微观问题（“狗的耳朵是什么颜色？”）触发细粒度模式。
*   **自适应 Token 聚合模块（AdaTA）**：
    *   **流程**：
        1.  **粒度引导池化**：根据控制器预测的 $\alpha$ 调整特征分辨率。
        2.  **关系感知聚类**：利用 mini-k-means 算法，结合空间和注意力特征，将相似的 Token 聚类为语义原型。
        3.  **特征精炼与筛选**：基于空间支持度、语义同质性和分布离散度对聚类进行评分，保留最具有信息量的 Top-K 个语义 Token。
*   **统一推理**：将原始像素级 Token 与 AdaTA 生成的语义 Token 拼接，共同输入 LLM，实现“像素-细粒度-粗粒度”的联合推理。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用 VQA**：SEED-Bench, A-OKVQA。
    *   **图像描述**：CC12M, Imagenet21K Recap。
    *   **复杂推理**：FLUX-Reason（包含 20 万训练子集）。
    *   **医疗领域**：SurgVLM（手术阶段识别、器械识别），用于验证跨域泛化性。
*   **Benchmark 与对比方法**：
    *   对比了主流视觉编码器：CLIP (ViT-L/14), SigLIP (ViT-L/16), DINOv2 (ViT-L/14), DINOv3 (ViT-L/16)。
    *   使用了两种 LLM 后端：Qwen-2.5-1.5B 和 Llama-3.2-3B。
    *   评估指标：Recall（准确率）、GPTscore（利用 GPT-4o 评分）、BERTscore、幻觉率（Hallucination Rate）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA H200 GPU**。
*   **训练细节**：
    *   训练时长：2 个 Epoch。
    *   Batch Size：128（每张卡 8，梯度累积）。
    *   优化器：AdamW，学习率 $2 \times 10^{-5}$。
    *   框架：基于 LLaVA 框架，冻结视觉骨干和 LLM，仅训练多模态投影器（Projector）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同类别的基准测试上进行了广泛评估，涵盖了从通用理解到专业医疗领域的任务。
*   **消融实验**：详细探讨了语义 Token 粒度对不同任务的影响（发现 A-OKVQA 偏好粗粒度，而推理任务偏好细粒度），并验证了控制器的有效性。
*   **客观性与公平性**：所有对比实验均在相同的 LLaVA 框架、数据集规模、模型参数量（约 0.3B-0.4B 视觉参数）和优化超参数下进行，确保了结果的客观性。此外，还引入了层级对齐分析（Layer-wise Alignment）来排除语言模型偏见。

### 6. 主要结论与发现
*   **性能飞跃**：Granulon 在推理准确率上比 CLIP 基础模型提升了约 **30%**。
*   **幻觉抑制**：相比 DINOv3，Granulon 在推理任务中的幻觉率降低了约 **20%**（相对降幅达 46%）。
*   **粒度适配性**：实验证明，视觉粒度的需求是任务相关的。Granulon 的控制器能有效学习这种映射，使模型在保持 Token 效率的同时获得更高精度。
*   **医疗泛化**：在未见过的医疗手术场景中，Granulon 的表现显著优于 CLIP 和原始 DINO，证明了其细粒度特征的鲁棒性。

### 7. 优点
*   **动态性**：首次将视觉粒度转化为受文本引导的可控维度，实现了“按需分配”视觉注意力。
*   **高效集成**：在单一前向传播中融合了多尺度特征，无需像传统方法那样使用多个复杂的视觉编码器。
*   **可解释性强**：通过层级对齐分析和可视化案例，清晰展示了模型如何通过增强细粒度证据来支持深层推理。

### 8. 不足与局限
*   **评估依赖**：高度依赖 GPT-4o 作为裁判（LLM-as-a-judge），虽然进行了人工校验，但仍可能存在自动评估器的固有偏见。
*   **计算开销**：虽然提高了效率，但自适应控制器和聚类过程相比固定 Token 方案增加了约 10% 的 Token 消耗。
*   **训练数据偏向**：主要在 FLUX-Reason 子集上训练，对于极度长尾或完全不同分布的视觉概念，可能仍存在识别局限。

（完）
